"""
Pytest configuration for browser UI tests
Uses pytest-playwright's built-in fixtures and options
"""
# No custom configuration needed - pytest-playwright provides:
# - --base-url option
# - --headed option  
# - browser fixtures
# - all necessary setup
